// Responisble for the logic. Initialize memory pool, find suitable block, split block, coalesce block
//
// Conceptually...
// init()
// allocate(size)
// deallocate(ptr)
// find_block(size)
// split_block(block, size)
// coalesce
